const Input = () => {
    const label = "Video";

    return (
        <div>
            <label htmlFor="mame">{label}</label>
            <br />
            <input type="text" id="name"/>
        </div>
    );
}

export default Input;